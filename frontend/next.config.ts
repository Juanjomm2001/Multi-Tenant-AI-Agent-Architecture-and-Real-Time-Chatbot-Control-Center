import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Configuración para el dashboard */
  devIndicators: false, // Esto quita la burbuja de la "N" y el menú blanco
};

export default nextConfig;
